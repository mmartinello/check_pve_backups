# Changelog

## 1.1

- Add automatic retry on retryable Proxmox API HTTP errors (e.g. `596`
  connection timeout), configurable via `--retry-times` and
  `--retry-interval`.
- Add README documentation.

## 1.0

First stable release.
